/**
 * @param {string} s
 * @param {number[]} indices
 * @param {string[]} sources
 * @param {string[]} targets
 * @return {string}
 */
//解题思路：暴力匹配
var findReplaceString = function (s, indices, sources, targets) {
  const n = indices.length

  for(let i=0;i<n;i++){
    if(s.indexOf(sources[i])){
      let pos=s.indexOf(sources[i])
      
    }
  }


}